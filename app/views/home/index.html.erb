<h1>Courses</h1>

<ul>
<% @courses.each do |course| %>
  <li>
    <%= link_to course.name, course %> - Syllabus
    <hr/>
    <h3>Topics</h3>
    <ol>
    <% course.topics.each do |topic| %>
      <li><%= topic.name %> </li>
    <% end %>
<% end %>

</ul>
