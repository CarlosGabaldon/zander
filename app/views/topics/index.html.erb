<h1>Listing topics</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @topics.each do |topic| %>
  <tr>
    <td><%= topic.name %></td>
    <td><%= topic.description %></td>
    <td><%= link_to 'Show', course_topic_path(@course, topic) %></td>
    <td><%= link_to 'Edit', edit_course_topic_path(@course, topic) %></td>
    <td><%= link_to 'Destroy', course_topic_path(@course, topic) , :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Topic', new_course_topic_path(@course) %>
